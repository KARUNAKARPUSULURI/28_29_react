ternary operator

{
    this.state.submittedData ? (
        <>
        {this.state.submittedData.name}
        {this.state.submittedData.email}
        {this.state.submittedData.email}
        </>
    ) : "Please fill form" 
}